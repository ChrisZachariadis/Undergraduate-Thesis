import {StyleSheet} from "react-native";

const smartwatchStyle = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#f5f5f5',
    },
    metricsContainer: {
        padding: 4,
    },
    row: {
        flexDirection: 'row',
        marginBottom: 8,
    },
});

export default smartwatchStyle;
